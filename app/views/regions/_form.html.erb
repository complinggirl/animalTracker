<%= form_for(@region, :url => sighting_regions_path(@sighting, @region)) do |f| %>


<div class="field">
  <%= f.label : %>
  <%= f.select :name, ['option1', 'option2'] %>
</div>

<div  class="actions">
<%= f.submit %>
</div>
  <% end %>
